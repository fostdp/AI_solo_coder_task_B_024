# 代码审查报告 - 半导体晶圆厂设备预防性维护平台

**审查日期**：2026-06-04  
**审查范围**：gRPC流管理、健康评分算法、工单状态机、前端DOM渲染性能  
**技术债数量**：8个（含具体文件名和行号）

---

## 一、gRPC流管理健壮性分析

### 1.1 现状评估

| 维度 | 实现情况 | 等级 | 说明 |
|------|----------|------|------|
| ✅ 断连清理 | 已实现 | 良好 | `context.add_callback(on_close)` + `finally` 双重清理 |
| ⚠️ 客户端重连 | 未实现 | 不足 | 设备端无重连逻辑，gRPC错误后静默失败 |
| ❌ 流控机制 | 未实现 | 严重 | 无流量控制，服务端可能被消息洪水淹没 |
| ❌ 背压处理 | 未实现 | 严重 | 双向流无背压，服务端处理不过来时消息堆积 |
| ⚠️ 心跳检测 | 未实现 | 不足 | 无gRPC健康检查或应用层心跳，TCP半连接无法检测 |
| ⚠️ 并发限制 | 部分实现 | 不足 | `ThreadPoolExecutor(max_workers=20)` 但无单IP流数限制 |

### 1.2 关键代码分析

**已实现的断连清理**（`grpc_server.py:305-336`）：
```python
def StreamSensorData(self, request_iterator, context):
    stream_id = id(context)
    with _streams_lock:
        _active_streams.add(stream_id)
    
    try:
        def on_close():
            with _streams_lock:
                _active_streams.discard(stream_id)
        context.add_callback(on_close)  # gRPC官方关闭回调
        
        for report in request_iterator:
            yield self.ReportSensorData(report, context)
    finally:
        with _streams_lock:
            _active_streams.discard(stream_id)  # 兜底清理
```
**优点**：双重保险（回调+finally），线程安全的连接追踪。  
**不足**：仅解决了内存泄漏，未解决可用性问题。

### 1.3 缺失的健壮性机制

#### (1) 客户端无自动重连
`simulator.py:113-124` 中设备端gRPC调用失败后仅计数，无重连策略：
```python
try:
    ack = stub.ReportSensorData(report, timeout=5)
except grpc.RpcError as e:
    error_count += 1
    # 无重连逻辑，数据直接丢失
```
**风险**：网络闪断后设备数据永久丢失，直到下一个30秒周期。

#### (2) 无服务端流控
`grpc_server.py:340` 线程池仅20个worker：
```python
server = grpc.server(futures.ThreadPoolExecutor(max_workers=20))
```
200台设备每30秒上报一次 → ~7 TPS，但双向流下每个stream占用一个线程直到关闭。200个设备用双向流的话线程池立即耗尽。

#### (3) 无背压处理
`StreamSensorData` 中 `yield` 不考虑下游消费速度：
```python
for report in request_iterator:
    ack = self.ReportSensorData(report, context)  # 可能阻塞（DB操作）
    yield ack  # 发送速度 = 接收速度，无缓冲
```
当DB慢时，客户端发送队列堆积，最终OOM。

#### (4) 无心跳检测
TCP半连接状态下（如客户端断电），gRPC服务端可能几小时都检测不到断连。`_active_streams` 计数失真。

---

## 二、健康评分算法合理性分析

### 2.1 算法现状

**核心公式**（`grpc_server.py:69-101`）：
```python
temp_dev = abs(temperature - baseline_temp) / baseline_temp
vib_dev  = abs(vibration - baseline_vib) / baseline_vib
pwr_dev  = abs(rf_power - baseline_pwr) / baseline_pwr

temp_score = max(0, 100 - temp_dev * 100)
vib_score  = max(0, 100 - vib_dev * 100)
pwr_score  = max(0, 100 - pwr_dev * 100)

health = 0.3*temp + 0.4*vib + 0.3*pwr  # 加权求和
```

**评分曲线**：偏离度0%→100分，偏离度100%→0分，线性衰减。

### 2.2 合理性评估

| 方面 | 评估 | 问题 |
|------|------|------|
| 权重分配 | 基本合理 | 振动0.4最高，符合半导体设备特性，但未验证 |
| 偏离度计算 | ⚠️ 过于简单 | 绝对值偏离，不考虑参数物理特性 |
| 评分函数 | ❌ 线性衰减不合理 | 偏离10%直接扣10分，偏离50%直接不及格 |
| 基线更新 | ❌ 缺失 | 基线设为NULL时校准一次，之后永不更新 |
| 基线算法 | ❌ 既非滑动窗口也非指数平滑 | 静态值，无法适应设备老化 |
| 型号差异 | ✅ 已处理 | 同型号均值校准（刚修复） |

### 2.3 严重问题

#### (1) 基线永不更新（技术债 #1）
**位置**：`grpc_server.py:76-87`
```python
if baseline_temp is None:
    bt, bv, bp = get_type_baseline(equipment_type)
    # 只在第一次为NULL时更新，后续即使有了24小时数据也永不重新校准
    cur.execute("UPDATE devices SET baseline_temperature = %s ...")
```
**问题**：
- 新设备用同型号均值后，积累了24小时真实数据，基线不会自动修正
- 设备运行半年后老化，参数漂移，但基线还是出厂值
- 既不是**滑动窗口**（如最近24小时均值），也不是**指数平滑**（`new = α*current + (1-α)*old`）

**建议**：每天凌晨用最近24小时数据做指数平滑更新：
```python
# 指数平滑（α=0.1，约10天收敛到新值）
new_baseline = 0.1 * avg_24h + 0.9 * old_baseline
```

#### (2) 评分函数过于严苛（技术债 #2）
**位置**：`grpc_server.py:97-99`
```python
temp_score = max(0, 100 - temp_dev * 100)  # 偏离1%扣1分
```
**问题**：
- 正常波动±5% → 直接扣5分 → 95分
- 温度350°C，波动17.5°C（5%）→ 95分，这在半导体设备中是正常的
- 建议用非线性评分：偏离<5%不扣分，5-20%线性扣分，>20%加速扣分

```python
# 改进建议
if dev < 0.05:
    score = 100
elif dev < 0.2:
    score = 100 - (dev - 0.05) / 0.15 * 30  # 5%~20%偏离扣0~30分
else:
    score = 70 - (dev - 0.2) / 0.8 * 70   # >20%加速扣分
```

#### (3) 不同型号参数差异处理不完善（技术债 #3）
**位置**：`grpc_server.py:45-57`
```python
def get_type_baseline(equipment_type):
    SELECT AVG(baseline_temperature) ... FROM devices WHERE equipment_type = %s
```
**已解决**：新设备用同型号均值。  
**仍存问题**：
- 同型号内部差异也可能很大（如新旧设备），用均值抹平了差异
- 没有考虑"同区域同型号"或"同批次"的更细粒度分组
- 没有异常值剔除：某台设备基线异常会拉低整个型号的均值

---

## 三、工单状态机完备性分析

### 3.1 状态机现状

**状态流转**（`main.py:213-261`）：
```
pending (待接单) → accepted (处理中) → completed (已完成) → verified (已验收)
```

**API实现**：
| API | 前置状态 | 后置状态 |
|-----|----------|----------|
| `/accept` | pending | accepted |
| `/complete` | accepted | completed |
| `/verify` | completed | verified |

### 3.2 完备性评估

| 方面 | 评估 | 问题 |
|------|------|------|
| 状态流转校验 | ✅ 已实现 | WHERE子句校验前置状态 |
| 自动创单去重 | ⚠️ 部分实现 | 仅检查pending/accepted，不检查同一天同类工单 |
| 手动创单入口 | ❌ 缺失 | 只有自动创单，工程师无法手动创单 |
| 工单取消 | ❌ 缺失 | 误报工单无法取消 |
| 工程师通知 | ⚠️ 不可靠 | 仅WebSocket推送，无持久化通知 |
| 超时处理 | ❌ 缺失 | 工单pending超过24小时无升级机制 |

### 3.3 严重问题

#### (1) 自动创单与手动创单无去重（技术债 #4）
**位置**：`grpc_server.py:223-230`
```python
cur.execute("""
    SELECT id FROM work_orders
    WHERE device_id = %s AND status IN ('pending', 'accepted')
    LIMIT 1;
""")
```
**问题**：
- 只防止了"待接单/处理中"的重复创单
- 如果工程师手动创建了一个工单，自动创单逻辑仍可能创建重复工单
- 同一天同一设备同一原因（健康评分低）可能创建多张工单
- 没有"已创建但被取消"的状态，误报后无法标记

**建议**：增加24小时窗口内的去重检查：
```sql
SELECT id FROM work_orders
WHERE device_id = %s 
  AND created_at >= NOW() - INTERVAL '24 hours'
  AND reason LIKE '%健康评分%'
LIMIT 1;
```

#### (2) 工程师通知不可靠（技术债 #5）
**位置**：`grpc_server.py:244-251`
```python
if _alert_callback:
    _alert_callback({
        "type": "work_order",
        "message": "..."
    })
```
**问题**：
- 仅通过WebSocket推送到前端浏览器
- 工程师不在线、手机没开网页 → 永远收不到通知
- 没有短信、邮件、企业微信等备份通知渠道
- 没有通知确认机制，系统不知道工程师是否已读

#### (3) 状态机缺失关键状态
缺少的状态：
- `cancelled` (已取消) - 误报工单
- `escalated` (已升级) - 超时未处理
- `rejected` (已驳回) - 验收不通过

缺少的流转：
- pending → cancelled（误报取消）
- verified → rejected（验收不通过，重回处理）
- pending → escalated（24小时无人接单，升级给主管）

---

## 四、前端DOM渲染性能分析

### 4.1 现状分析

**核心渲染逻辑**（`app.js:136-161`）：
```javascript
function renderGrid(data) {
    var filtered = filterDevices();
    var html = "";
    filtered.forEach(function (d) {
        html += '<div class="device-cell" ...>';
        html += '...</div>';
    });
    gridEl.innerHTML = html;  // 全量替换
    
    gridEl.querySelectorAll(".device-cell").forEach(function (cell) {
        cell.addEventListener("click", ...);  // 重新绑定事件
    });
}
```

**刷新频率**：每30秒全量刷新一次（`app.js:522-528`）。

### 4.2 性能开销分析

| 操作 | 开销 | 说明 |
|------|------|------|
| HTML字符串拼接 | 低 | 200个元素，字符串拼接很快 |
| innerHTML全量替换 | 中 | 销毁200个旧DOM + 创建200个新DOM |
| 浏览器重排 (Reflow) | 高 | 200个元素同时插入，触发至少1次全量布局 |
| 浏览器重绘 (Repaint) | 中 | 200个色块背景色变化 |
| 事件监听器重新绑定 | 中 | 200个addEventListener调用 |

### 4.3 性能问题量化

**200个元素全量刷新的开销**：

```
每次刷新:
├── 字符串拼接: ~0.1ms (可忽略)
├── innerHTML:   ~2-5ms (销毁+创建)
├── 重排(Reflow): ~5-15ms (最耗时，布局计算)
├── 重绘(Repaint): ~2-5ms (像素填充)
└── 事件绑定:   ~1-3ms (200次addEventListener)
──────────────────────────────
总计:            ~10-28ms / 30s
```

**问题**：
- 每次刷新全部销毁重建，无法利用CSS transition做平滑过渡
- 200个事件监听器每次都重新绑定，内存产生临时垃圾
- 重排开销随设备数线性增长：500台设备 → ~30-50ms重排

### 4.4 严重问题

#### (1) innerHTML全量替换导致重排开销大（技术债 #6）
**位置**：`app.js:153`
```javascript
gridEl.innerHTML = html;  // 全量替换
```
**问题**：
- 即使只有1台设备的健康评分变化，也要重建全部200个DOM
- 每次刷新丢失元素状态（如hover、focus）
- 无法做颜色渐变动画（从绿到红的平滑过渡）

**建议**：使用diff更新，只修改变化的元素：
```javascript
// 改进方案：按device-id查找，只更新属性
filtered.forEach(function(d) {
    var cell = gridEl.querySelector('[data-device-id="' + d.device_id + '"]');
    if (cell) {
        // 只更新变化的属性，不重建DOM
        cell.style.background = healthToColor(d.health_score);
        cell.querySelector('.device-score').textContent = score;
        cell.className = computeClass(d);
    } else {
        // 新增元素才创建
        gridEl.appendChild(createCell(d));
    }
});
```

#### (2) 事件监听器未做事件委托（技术债 #7）
**位置**：`app.js:155-160`
```javascript
gridEl.querySelectorAll(".device-cell").forEach(function (cell) {
    cell.addEventListener("click", function () { ... });
});
```
**问题**：
- 200个元素 × 1个监听器 = 200个监听器
- 每次刷新全部重新绑定，产生GC压力
- 新增元素需要重新绑定

**建议**：使用事件委托，只在父元素绑定1个监听器：
```javascript
// 只需绑定一次，在init()中调用
gridEl.addEventListener("click", function(e) {
    var cell = e.target.closest(".device-cell");
    if (cell) {
        var id = cell.getAttribute("data-device-id");
        openSidebar(id);
    }
});
```

#### (3) 定时器刷新未做防抖（技术债 #8）
**位置**：`app.js:522-528`
```javascript
refreshTimer = setInterval(function () {
    loadDevices();      // 触发renderGrid
    loadSummary();
    if (selectedDeviceId) {
        openSidebar(selectedDeviceId);  // 又触发一次renderGrid
    }
}, 30000);
```
**问题**：
- 如果有侧边栏打开，`openSidebar` 会在 `loadDevices` 的 `renderGrid` 之后**再调用一次** `renderGrid`
- 30秒内连续两次全量刷新，性能开销翻倍
- 没有检查标签页是否可见，后台标签页也在刷

**建议**：使用`requestAnimationFrame`合并更新，检查`document.hidden`。

---

## 五、技术债清单（共8个）

| 编号 | 技术债描述 | 文件 | 行号 | 严重程度 | 修复成本 |
|------|----------|------|------|----------|----------|
| #1 | 基线永不更新，既非滑动窗口也非指数平滑，无法适应设备老化 | `grpc_server.py` | 76-87 | 🔴 高 | 中 |
| #2 | 健康评分线性衰减过于严苛，正常波动也会扣分 | `grpc_server.py` | 97-99 | 🟠 中 | 低 |
| #3 | 同型号基线均值未做异常值剔除，个别异常设备拉低全局 | `grpc_server.py` | 48-51 | 🟠 中 | 中 |
| #4 | 工单去重仅检查pending/accepted，不检查24小时内同类工单 | `grpc_server.py` | 223-230 | 🟠 中 | 低 |
| #5 | 工程师通知仅依赖WebSocket，无持久化和备份渠道 | `grpc_server.py` | 244-251 | 🔴 高 | 高 |
| #6 | 前端用innerHTML全量替换200个DOM，重排开销大 | `app.js` | 153 | 🟠 中 | 中 |
| #7 | 前端事件未做委托，200个监听器每次刷新重新绑定 | `app.js` | 155-160 | 🟡 低 | 低 |
| #8 | 前端定时刷新未防抖，打开侧边栏时30秒内双重渲染 | `app.js` | 522-528 | 🟡 低 | 低 |

---

## 六、修复优先级建议

### 立即修复（本周内）
1. **#5 工程师通知可靠性** - 业务风险最高，漏通知可能导致设备宕机
2. **#1 基线更新策略** - 直接影响健康评分准确性，错误的评分导致错误的维保

### 近期修复（1-2周）
3. **#6 DOM渲染优化** - 200台还好，扩展到500+会明显卡顿
4. **#4 工单去重增强** - 减少工单噪音，降低工程师负担
5. **#2 评分函数优化** - 提升评分合理性，减少误告警

### 长期优化（后续版本）
6. **#3 基线异常值剔除**
7. **#7 事件委托优化**
8. **#8 刷新防抖优化**

---

## 七、gRPC流管理补充建议

除了上述技术债，gRPC流还需补充：

1. **客户端重连**：`simulator.py` 添加指数退避重连（1s→2s→4s→8s，最大30s）
2. **服务端心跳**：在 `StreamSensorData` 中添加应用层心跳（每10秒发送ping）
3. **流控参数**：gRPC Server添加 `grpc.max_receive_message_length`、`grpc.max_concurrent_streams`
4. **背压处理**：使用带缓冲的channel，当缓冲满时向客户端发送`flow_control`消息
5. **监控告警**：`_active_streams` 数量偏离设备数±10%时告警

---

## 八、总结

本项目在核心业务逻辑上基本可用，但在**健壮性**（gRPC流控、通知可靠性）、**算法合理性**（基线更新、评分函数）、**前端性能**（DOM渲染）三个方面存在明显技术债。建议按照优先级逐步修复，特别是工程师通知和基线更新这两个高优先级问题。
